# Copy this file to .env and fill in values.
# .env is gitignored — never commit secrets.
#
# Note: Myelin does NOT read .env automatically.
# These are only used if you manually source them or use direnv.

# ── Benchmark evaluation (LongMemEval GPT-4o judge) ─────────────
OPENAI_API_KEY=
